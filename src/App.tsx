import React from 'react';
import styled from 'styled-components';
import './App.css'

const AppContainer = styled.div`
  padding: 20px; 
`;

const App: React.FC = () => {

  return (
    <AppContainer>

    </AppContainer>
  );
};

export default App
